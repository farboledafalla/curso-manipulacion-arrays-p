// Retornar un array multiplicado por 2 cada elemento

const numeros = [2, 4, 6, 8];

console.log(numeros.map((item) => item * 2));

// const porDos = numeros.map((numItem) => {
//    return numItem * 2;
// });

// console.log(numeros);
// console.log(porDos);
